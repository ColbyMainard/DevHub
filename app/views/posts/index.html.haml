%h2 All Posts
%table#posts.table.table-striped.col-md-12
  %thead
    %tr
      %th Post Title
      %th User
      %th Last Updated
      %th More Info
  %tbody
    - @posts.each do |post|
      %tr
        %td
          = link_to "#{post.post_title}", list_posts_path
        %td
          = post.username
        %td
          = post.updated_at
= link_to 'Add new post', new_post_path, :class => 'btn btn-primary'